<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" /> <!-- Background color -->
    <corners android:radius="50dp" /> <!-- Corner radius -->
    <padding
        android:left="15sp"
        android:right="15sp"
        android:top="2sp"
        android:bottom="2sp" />
    <stroke
        android:width="1dp"
        android:color="@color/main_blue" /> <!-- Border color and width -->
</shape>
